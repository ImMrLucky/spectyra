{
  "name": "@spectyra/learning",
  "version": "0.1.0",
  "description": "Local and global learning system. Per-app learning stays local; global aggregate learning uses non-sensitive metrics only.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:learning-loop": "tsx src/__tests__/learning-phase5.test.ts"
  },
  "dependencies": {
    "@spectyra/canonical-model": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.7.1",
    "typescript": "~5.4.0"
  },
  "license": "MIT"
}
