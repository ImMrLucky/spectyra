/**
 * Example OpenClaw `models.providers` + default agent config.
 * Canonical JSON lives in @spectyra/integration-metadata.
 */
export { OPENCLAW_CONFIG_JSON as OPENCLAW_CONFIG_EXAMPLE_JSON } from "@spectyra/integration-metadata";
