{
  "buildCommand": "pnpm --filter web build --configuration production",
  "outputDirectory": "apps/web/dist/web/browser",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
