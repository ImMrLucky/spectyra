{
  "buildCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && pnpm install && pnpm --filter web build --configuration production",
  "outputDirectory": "apps/web/dist/web/browser",
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
