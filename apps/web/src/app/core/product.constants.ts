/**
 * Product copy and defaults. Billing amount is enforced by Stripe (STRIPE_PRICE_ID on API); keep labels in sync.
 */
export const SPECTYRA_TRIAL_DAYS = 60;
export const SPECTYRA_MONTHLY_PRICE_LABEL = "$19.99/month";
