export const environment = {
  production: true,
  // Use relative URL - Netlify will proxy /v1/* to your API via netlify.toml redirects
  // Update the redirect in netlify.toml to point to your Railway API URL
  apiUrl: '/v1',
};
