[build]
  base = "apps/web"
  command = "cd ../.. && pnpm install && pnpm --filter web build"
  publish = "dist/web/browser"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "8.15.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
